# Auth/Tenant Phase 0 Preflight

Last updated: 2026-07-10

Status: completed read-only preflight. This document and the companion audit do
not authorize an auth provider, schema migration, owner backfill, RLS policy,
query behavior change, user write path, or recommendation implementation.

## Outcome

The current deployment is still a single imported portfolio behind Basic Auth.
Basic Auth is an access gate, not an identity provider, and account codes such
as `brokerage`, `isa`, `irp`, and `all` are portfolio partitions, not tenants.

The canonical future contract is:

| Concern | Decision |
| --- | --- |
| Internal user table | `app_users` |
| Internal user key | `app_users.id uuid` |
| Provider mapping table | `auth_identities` |
| User-owned row key | `owner_user_id uuid NOT NULL REFERENCES app_users(id)` |
| Existing varchar owners | Legacy evidence only; not internal UUIDs |
| Initial imported data | Assign to one explicitly approved initial app user |
| Shared market data | One shared copy; no tenant owner column |
| Basic Auth | Deployment access only; never supplies `owner_user_id` |

No current row is canonical-owner ready. The read-only database audit reports
757 user-owned rows, all of which require an approved owner migration before
multi-user access can be enabled.

## Reproducible Audit

Run:

```powershell
npm run audit:tenant-ownership
```

The audit performs five `SELECT` statements, classifies every public base
table, reports only sanitized counts/metadata, and fails if a table is missing
from the policy. It never prints owner values and never writes to the database.

Observed on 2026-07-10:

- 22 public base tables
- 0 foreign keys
- 14 `user_owned` tables, 757 rows
- 7 `shared_reference` tables, 27,806 rows
- 1 `admin_system` table, 65 rows
- 0 `unresolved` tables
- 0 rows with the final non-null UUID owner contract

Counts are operational evidence, not migration guards by themselves. A future
migration must re-run and explicitly approve fresh counts.

## Complete Table Classification

| Table | Rows | Classification | Current owner evidence | Target mapping |
| --- | ---: | --- | --- | --- |
| `assets` | 17 | `user_owned` | nullable `created_by_id varchar`, all null | Add canonical owner; deprecate `created_by_id` |
| `accounts` | 4 | `user_owned` | nullable `owner_user_id varchar`, one legacy value | Convert to canonical owner |
| `asset_groups` | 1 | `user_owned` | nullable `owner_user_id varchar`, one legacy value | Convert to canonical owner |
| `asset_group_members` | 1 | `user_owned` | nullable `owner_user_id varchar`, one legacy value | Convert to canonical owner |
| `fx_rates` | 468 | `shared_reference` | none | Shared FX evidence |
| `asset_price_snapshots` | 11,329 | `shared_reference` | none | Shared ticker/date price evidence |
| `market_data_sync_runs` | 65 | `admin_system` | none | Operator-only job evidence |
| `live_price_quotes` | 15 | `shared_reference` | none | Shared short-lived quote cache |
| `benchmark_snapshots` | 1,519 | `shared_reference` | none | Shared benchmark history |
| `etf_masters` | 1,202 | `shared_reference` | none | Shared ETF reference universe |
| `etf_holdings` | 10,872 | `shared_reference` | none | Shared ETF look-through history |
| `event_ledger_entries` | 51 | `user_owned` | none | Add canonical owner |
| `market_regime_daily` | 69 | `user_owned` | none; account-specific derived rows | Add canonical owner |
| `global_market_factors` | 2,401 | `shared_reference` | none | Shared macro/factor evidence |
| `goals` | 1 | `user_owned` | nullable `owner_user_id varchar`, one legacy value | Convert to canonical owner |
| `transactions` | 14 | `user_owned` | nullable `owner_user_id varchar`, one legacy value | Convert to canonical owner |
| `fixed_transactions` | 7 | `user_owned` | nullable `owner_user_id varchar`, one legacy value | Convert to canonical owner |
| `monthly_incomes` | 12 | `user_owned` | nullable `owner_user_id varchar`, one legacy value | Convert to canonical owner |
| `account_balance_snapshots` | 3 | `user_owned` | none | Add canonical owner |
| `daily_portfolio_snapshots` | 90 | `user_owned` | none | Add canonical owner |
| `daily_position_snapshots` | 486 | `user_owned` | none | Add canonical owner |
| `settings` | 1 | `user_owned` | none | Add canonical owner; one settings row per user |

`asset_price_snapshots.asset_id` is not an authorization boundary. This table
is shared by ticker/date, and its nullable asset link must not be used to infer
an owner. A later cleanup may deprecate that link or document it strictly as
source metadata.

## Canonical Identity Model

Proposed `app_users` fields:

| Field | Contract |
| --- | --- |
| `id` | UUID primary key generated by varda-labs |
| `email` | Nullable/unique only after provider policy is chosen |
| `display_name` | Optional product label |
| `status` | Active/disabled lifecycle state |
| `created_at`, `updated_at` | Internal timestamps |

Proposed `auth_identities` fields:

| Field | Contract |
| --- | --- |
| `id` | UUID primary key |
| `app_user_id` | Non-null FK to `app_users.id` |
| `provider` | Provider namespace |
| `provider_subject` | Provider-stable external subject |
| unique key | `(provider, provider_subject)` |

The product must resolve a provider subject to an internal UUID on the server.
External provider IDs, email addresses, Basic Auth usernames, Base44 owner
strings, and `base44-import` labels must never become row ownership keys.

## Initial Owner Backfill Plan

The future migration must be dry-run/approval gated and use an explicit
`--initial-owner-id <uuid>` (or equivalent reviewed configuration). It must not
invent or infer the owner.

Recommended sequence:

1. Re-run row/null/distinct-owner guards and stop on unexpected values.
2. Create one reviewed `app_users` row for the imported portfolio.
3. Add nullable canonical owner columns to ownerless user tables.
4. For tables already named `owner_user_id`, rewrite the single approved legacy
   marker to the reviewed UUID text, then alter the column to UUID.
5. For `assets`, populate a new `owner_user_id`; retain `created_by_id` only as
   legacy evidence until a later cleanup.
6. Backfill every user-owned row to the same approved initial owner.
7. Validate row counts, null counts, relationship ownership, and uniqueness.
8. Add owner-aware unique indexes, `NOT NULL`, and FKs only after validation.
9. Deploy owner-filtered application reads/writes before enabling a second user.

Per-table strategy:

| Table group | Backfill action |
| --- | --- |
| `accounts`, `asset_groups`, `asset_group_members`, `goals`, `transactions`, `fixed_transactions`, `monthly_incomes` | Guard the current one-value legacy domain, rewrite only that approved domain to the initial UUID, cast to UUID, then enforce FK/non-null |
| `assets` | Add `owner_user_id`, assign the explicit initial UUID, validate account/group relations, then deprecate `created_by_id` |
| `event_ledger_entries`, `market_regime_daily`, snapshots, `settings` | Add nullable owner, assign the explicit initial UUID, validate counts and relations, then enforce FK/non-null |
| Shared reference tables | No owner backfill and no tenant duplication |
| `market_data_sync_runs` | No user owner; keep behind an admin/system role |

## Owner Propagation Inventory

### Read paths

| Surface | Current entry points | Future owner boundary |
| --- | --- | --- |
| `/` and `/today` | `src/app/page.tsx`, `src/app/today/page.tsx`, `src/lib/portfolio-dashboard.ts`, `src/lib/portfolio-movement.ts` | Filter accounts, assets/groups, settings, events, and daily snapshots by current owner; join shared price/FX/live quote evidence without owner inference |
| `/history` | `src/db/queries/history-balance.ts` | Filter balance and portfolio snapshots by owner before account/lane filters |
| `/portfolio/structure` | `src/db/queries/portfolio-structure.ts` | Filter assets/groups/members/settings by owner; keep quote/FX reads shared |
| `/portfolio/risk` | `src/db/queries/portfolio-risk.ts` | Filter current assets by owner; price snapshots and FX remain shared |
| `/market` | `src/db/queries/market-context.ts` | Filter `market_regime_daily` by owner; benchmark/global-factor reads remain shared |
| `/etfs` | `src/db/queries/etf-holdings.ts` | Shared reference read; no tenant predicate |
| `/admin/market-sync` | `src/db/queries/admin-market-sync-status.ts` | Admin role only; cross-user aggregate evidence must never become a user-session query |

All user-facing query helpers should take a server-derived `TenantContext` or
internal user UUID. URL search params such as `account=isa` may narrow data
inside that owner boundary but cannot select an owner.

### Write paths that can currently create ownerless or legacy-owner rows

| Writer | Risk and future requirement |
| --- | --- |
| `scripts/import-base44-core.mjs` | Uses legacy string owner fields and lacks a canonical asset owner; require explicit initial owner |
| `scripts/import-base44-cashflow-goals.mjs` | Writes legacy string owners; require explicit initial owner |
| `scripts/import-base44-events.mjs` | Event rows have no owner; require explicit initial owner |
| `scripts/import-base44-history.mjs` | User snapshots have no owner; FX rows remain shared |
| `scripts/import-base44-settings.mjs` | Settings row has no owner; require explicit initial owner |
| `scripts/import-base44-market-context.mjs` | Regime rows require owner; global factors remain shared |
| `src/lib/snapshots/daily.ts` | Reads all portfolios and writes ownerless snapshots; future planner must partition by owner and propagate it transactionally |
| `src/app/api/entities/*` | Admin compatibility CRUD can write legacy/null ownership; keep admin-only until commands require an explicit authorized owner |

Market price, FX, ETF, benchmark, and live-quote synchronization stays shared
and administrative. It must not duplicate provider calls per user.

## Recommendation Ownership Contract

Future `recommendation_runs` and `recommendation_items` are user-owned:

- both require `owner_user_id uuid NOT NULL`;
- item owner must equal its parent run owner;
- `account` and nullable `account_id` are portfolio scope, not tenant identity;
- recommendation reads start with owner and then apply account/date filters;
- import, if later approved, requires the explicit initial owner;
- execution remains a separate owner-checked command/event path.

## Two-User Isolation Fixture Plan

Create deterministic users A and B with deliberately colliding account codes,
ticker symbols, dates, and labels. This catches missing owner predicates that
would be hidden by globally unique fixture values.

Required coverage:

1. Each user owns accounts, assets/groups, settings, events, snapshots, and
   regime rows; shared market rows exist only once.
2. Dashboard, today movement, history, structure, risk, and market-context
   query helpers return only the selected user's financial rows.
3. Shared prices, FX, ETF, benchmark, and factor evidence is available to both
   users without being duplicated.
4. Counts, labels, totals, and unmatched snapshot fallbacks contain no
   cross-user values.
5. User writes reject a missing owner and ignore/reject a browser-supplied
   owner; ownership comes from server context.
6. Recommendation runs/items require an owner and reject parent/item owner
   mismatch.
7. An explicit admin fixture may see cross-user operational aggregates.
8. A later RLS integration suite sets users A and B in separate transactions
   and proves the same isolation at the database layer.

## RLS ADR Direction

RLS remains deferred until app-level owner filtering works and the Neon/Drizzle
transaction behavior is proven.

Future role model:

- migration/owner role: owns schema and runs reviewed migrations;
- application role: does not own tables and has no `BYPASSRLS`;
- admin job role: separate from user sessions and explicitly authorized;
- public browser: never connects to Postgres directly.

Future request identity direction:

1. Server session resolves provider identity to `app_users.id`.
2. App-level queries always include `owner_user_id` predicates.
3. In one database transaction, set a transaction-local identity with
   `set_config('app.current_user_id', <uuid>, true)`.
4. RLS `USING` and `WITH CHECK` compare `owner_user_id` with a null-safe cast of
   that setting.
5. Prove Neon HTTP/Drizzle keeps the setting and protected query in the same
   transaction/connection before any policy is enabled.
6. Shared tables receive read grants without owner RLS; admin/system tables are
   not granted to the application role.

The future policy shape may use:

```sql
owner_user_id = nullif(current_setting('app.current_user_id', true), '')::uuid
```

This is an ADR direction only, not executable migration SQL.

## Ordered Gates

1. Approve this ownership classification and explicit initial-owner policy.
2. Select an auth provider and session strategy.
3. Draft exact `app_users`/`auth_identities`/owner-column Drizzle schema and SQL,
   still without applying it.
4. Add two-user application-level query/write fixtures.
5. Apply the reviewed identity and owner migration plus initial-owner backfill.
6. Deploy owner-filtered reads and writes; prove application isolation.
7. Prove transaction-scoped identity and then evaluate RLS in a separate gate.
8. Only after those gates, add a second real user or user-facing mutations.
